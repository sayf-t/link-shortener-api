# Link Shortener API

URL shortener microservice built with Rails 8.1 and PostgreSQL.

## Setup

```bash
bundle install
bin/rails db:create db:migrate
bin/rails server
```

## Tests

```bash
bundle exec rspec
```
