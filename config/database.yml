default: &default
  adapter: postgresql
  encoding: unicode
  max_connections: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: link_shortener_api_development

test:
  <<: *default
  database: link_shortener_api_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
